[pytest]
testpaths = tests
# genlayer-test, when installed, registers auto-loading plugins that read any
# gltest config in the working directory; these tests need none of them.
addopts = -p no:cacheprovider
