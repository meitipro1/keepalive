# Studio Next (chain 61997), where Keepalive was first built and evaluated.
# Consensus v0.6 needs the release-candidate SDK; it cannot read Studionet.
# Use it in a separate virtual environment and set KEEPALIVE_NETWORK=studio-next.
genlayer-py==0.19.0rc2
genvm-linter==0.11.1rc2
pytest>=8
